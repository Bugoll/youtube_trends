PyYAML>=6.0              # YAML frontmatter in .md packages
yt-dlp>=2024.1.0         # live YouTube metrics (no API key needed)
anthropic>=0.50          # Claude API for LLM enrichment (optional — skipped if key not set)
