# Параметры обработки. Пайплайн рассчитан на graceful-fallback: если тяжёлые
# пакеты не установлены, используется встроенный TF-IDF на чистом Python.

PyYAML>=6.0                 # парсинг YAML-frontmatter в .md-пакетах

# Семантические эмбеддинги для карты связей идей (основной бэкенд).
# Тянет torch — в CI это нормально, локально можно поставить опционально.
sentence-transformers>=2.2.2

# Запасной лексический бэкенд (быстрый, лёгкий) — используется, если
# sentence-transformers недоступен.
scikit-learn>=1.3
numpy>=1.24
